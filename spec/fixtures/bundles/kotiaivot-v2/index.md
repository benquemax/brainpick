---
okf_version: "0.1"
---

# Kotiaivot (format 2)

A format-2 brain with a `conventions/` folder the template stamped
`type: decision`, one page the author typed by hand, and no index — the
fixture for `migrate --to 3`.
