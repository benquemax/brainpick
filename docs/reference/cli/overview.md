---
type: reference
about: thing
title: "brainpick overview"
description: "One screen of the whole brain — counts, tier status and every doc — the brain_overview tool as a CLI verb."
tags: [cli, spec]
timestamp: 2026-09-14T15:00:00Z
---

# brainpick overview

`brainpick overview [--root DIR]` prints one screen of the whole brain: the
bundle name, document/tag/entity counts, tier availability, and the index tree
with one-sentence descriptions. In a brain a `conventions (these apply to
you):` block comes before `skills (read before improvising):`, one line each
as `Title (path) — description` ([Conventions](../../conventions.md)). Add
`--json` for the raw MCP payload. It is the
orientation call — the progressive-disclosure root — and, like the other read
mirrors, never compiles.

It mirrors [brain_overview](../mcp/brain-overview.md); the counts and tiers it
shows are [the tiers](../../the-tiers.md). Back to [CLI reference](../../reference-cli.md).
