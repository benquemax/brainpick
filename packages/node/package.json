{
  "name": "brainpick",
  "version": "0.1.0",
  "//": "private until the 0.1 release decision — publishing to npm is a release-time act, flip this then",
  "private": true,
  "description": "pick your agent's brain — compile and serve OKF knowledge bundles (native Node engine)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "brainpick": "dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "smol-toml": "^1.3.1",
    "yaml": "^2.8.0"
  },
  "//optionalDependencies": "T2's vector store (spec/30) — absence degrades T2 with an instruction, never breaks T1. apache-arrow is @lancedb/lancedb's peer (schema construction).",
  "optionalDependencies": {
    "@lancedb/lancedb": "^0.31.0",
    "apache-arrow": "^18.1.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
